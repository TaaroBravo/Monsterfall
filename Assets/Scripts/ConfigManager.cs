using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;

public class ConfigManager : MonoBehaviour {

	void Awake ()
    {   
        //if(GameObject.FindObjectsOfType<ConfigManager>().Count()>1)
        //    Destroy(GameObject.FindObjectsOfType<ConfigManager>().First());
        //DontDestroyOnLoad(gameObject);
	}

	void Update ()
    {
		
	}
}
